<h1>Game List</h1>

<% if flash[:notice] %>
    <p><%= flash[:notice] %></p>
<% end %>

<table class="table table-striped">
    <tr>
        <th>Game</th>
        <th>Players</th>
        <th colspan="1"></th>
    </tr>

    <% @games.each do |game| %>
        <tr>
            <td><%= game.id %></td>
            <td><%= game.player_num %></td>
            <td><%= link_to 'Join', games_password_path({:id => game.id}) %></td>
        </tr>
    <% end %>
</table>